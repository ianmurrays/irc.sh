#!/usr/bin/env bash

SHIRCDIR="$(dirname $0)/../io"

HOST=$1
PORT=$2
NICK=$3

mkdir -p "$SHIRCDIR"

rm -f "$SHIRCDIR/cmd.in"
mkfifo "$SHIRCDIR/cmd.in"

nc $HOST $PORT <> "$SHIRCDIR/cmd.in" > "$SHIRCDIR/main.out" &

cat >> "$SHIRCDIR/cmd.in" <<EOF
NICK $NICK
USER $NICK 8 * : $NICK
EOF
